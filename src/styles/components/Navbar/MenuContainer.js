import styled from 'styled-components';

const MenuContainer = styled.div`
  width: fit-content;

  figure {
    width: 12.5px;
    height: 12.5px;

    img {
      width: 100%;
      height: 100%;
    }
  }
`;

export default MenuContainer;